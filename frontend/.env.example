# Copy this file to .env and set your values
VITE_API_URL=http://localhost:8080/api/v1

# Dev only: where Vite proxies /api (must match Spring server.port). Default is 8080 if unset.
# VITE_DEV_PROXY_TARGET=http://localhost:8085
